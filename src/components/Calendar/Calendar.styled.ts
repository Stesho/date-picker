import styled from 'styled-components';

export const CalendarWrapper = styled.div`
  position: relative;
  width: 250px;
  padding: 10px;
  background: #fff;
  border-radius: 8px;
  border: 1px solid #ddd;

  user-select: none;
`;
